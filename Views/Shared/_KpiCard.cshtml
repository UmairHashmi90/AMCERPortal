@model ERPaperless.Models.KpiCardViewModel

<article class="kpi-card @(string.IsNullOrWhiteSpace(Model.ToneClass) ? "tone-primary" : Model.ToneClass)">
    <p class="kpi-title">@Model.Title</p>
    <h3 class="kpi-value">@Model.Value</h3>
    <p class="kpi-subtitle">@Model.Subtitle</p>
</article>
